Summarize the following task into exactly one sentence of at most 60 characters. Describe what the task does, not how. Use plain language — no jargon, no markdown, no quotes. Output only the sentence, nothing else.

Task content:

{{.TaskContent}}